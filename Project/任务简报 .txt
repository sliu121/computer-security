1. 三个文件：
	result记录了候选人名字和票数
	hisotry 记录了reg代码和投票日期
	voterinfo 记录了voter名字和voter的reg代码

2. 建立可供多人在线的服务器

3. 输入voter 姓名和他的reg代码，用服务器公钥加密voter的name 和 reg 代码，用voter的私钥加密voter的名字 ---（如何实现?)
	a>. 加密，

4. 如果成立，输出界面 1 -4
	1. vote
	2. My vote history
	3. Election result 
	4. Quit

5. 输入1， 服务器检查history文件是否有投票信息，有的话，return已vote. 如果没有，弹出选人界面。
   **选人的结果要用加密的方法将内容从客户端传达到服务器上！！！** -- （这里的内容和上面相同）
	a>. 弹出选人界面，根据选项，候选人身后的票数要增加；
	b>. 将voter的reg代码和投票的时间存储到history文件中；
	c>. 如果投票人是最后一个投票的选手，直接弹出结果；（考虑一下在这里删除信息？ 或者每次重连服务器的时候重新生成result和history？）
	d>. 如果不是最后投票选手，不弹出结果。

6. 输入2，服务器调出voter的投票纪录。--（如果没有投票，那么这个记录该怎么输出?）

7. 输入3，服务器检查result，是不是三行内容，如果是，那么就输出结果，如果不是，那么输出错误。--（这里需要我们判断result）

8. 输入4，退出服务器。